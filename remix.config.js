/**
 * @type {import('@remix-run/dev/config').AppConfig}
 */
const config = {
  appDirectory: 'app',
  assetsBuildDirectory: 'public/build',
  publicPath: '/build/',
  ignoredRouteFiles: ['**/.*'],
};

export default config;
